# Copyright (c) 2026 DownStreamTech (https://downstreamtech.net)
# Developed by: Richard R. Ayuyang, PhD
#               Professor II, CSU
# All rights reserved.

services:
  ntp:
    build: ./ntp
    ports:
      - "0.0.0.0:123:123/udp"
    read_only: true
    tmpfs:
      - /var/run/chrony:size=1m
      - /var/lib/chrony:size=4m
      - /var/log/chrony:size=2m
      - /tmp:size=1m
    cap_drop:
      - ALL
    cap_add:
      - SYS_TIME
      - NET_BIND_SERVICE
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 32m
          cpus: "0.10"
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

  unbound:
    build: ./unbound
    read_only: true
    tmpfs:
      - /tmp:size=2m
      - /var/lib/unbound:size=4m,uid=100,gid=101
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - SETUID
      - SETGID
      - DAC_READ_SEARCH
      - DAC_OVERRIDE
    restart: unless-stopped
    volumes:
      - ./data:/data
    deploy:
      resources:
        limits:
          memory: 128m
          cpus: "0.5"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  sinkhole:
    build:
      context: .
      dockerfile: sinkhole/Dockerfile
    depends_on:
      - unbound
    ports:
      - "0.0.0.0:53:53/udp"
      - "0.0.0.0:53:53/tcp"
    environment:
      HOST_IP: ${HOST_IP:-}
      HTTP_PORT: ${HTTP_PORT:-80}
      TZ: ${TZ:-Asia/Manila}
      ROOT_PATH: /richsinkhole
      # Container-name prefix (Compose project name). Override in .env if you
      # rename the project; container_names.py picks it up automatically.
      RS_CONTAINER_PREFIX: ${COMPOSE_PROJECT_NAME:-richsinkhole}
    volumes:
      - ${DATA_DIR:-./data}:/data
      # Local SD-backed data — all critical databases and config.
      # sinkhole.db + blocklist.db + geoip + config on fast local storage
      # so DNS keeps working even when NAS is offline.
      # /data (NAS) is only used for updater status files and backups.
      - ${LOCAL_DATA:-./local-data}:/local
      - ${LOCAL_DATA:-./local-data}/config:/config
      - ./updater/sources.yml:/updater/sources.yml
      - ./nginx/certs:/certs:ro
      - ./nginx/conf.d:/nginx/conf.d
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ${BACKUP_DIR:-/mnt/nas/richsinkhole-backups}:/mnt/nas/richsinkhole-backups
      - /usr/local/bin/sinkhole-backup.sh:/usr/local/bin/sinkhole-backup.sh:ro
    dns:
      - 9.9.9.9
      - 1.1.1.1
    restart: unless-stopped
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          memory: 640m
          cpus: "2.0"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:8080/health')\" 2>/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  nginx:
    image: nginx:1.27-alpine
    read_only: true
    tmpfs:
      - /var/cache/nginx:size=16m
      - /var/run:size=1m
      - /tmp:size=2m
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
      - CHOWN
      - SETUID
      - SETGID
      - DAC_READ_SEARCH
    security_opt:
      - no-new-privileges:true
    ports:
      - "${HTTP_PORT:-80}:80"
      - "443:443"
    volumes:
      - ./nginx/${NGINX_CONFIG:-nginx-standalone.conf}:/etc/nginx/nginx.conf:ro
      - ./nginx/404.html:/usr/share/nginx/404.html:ro
      - ./nginx/50x.html:/usr/share/nginx/50x.html:ro
      - ./nginx/certs:/etc/nginx/certs:ro
      - ./nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - sinkhole
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO /dev/null http://localhost:80/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 64m
          cpus: "0.25"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

